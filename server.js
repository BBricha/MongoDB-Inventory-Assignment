const express = require('express');
const dotenv = require('dotenv');
dotenv.config(); 

const {postInventories} = require()

require('./config/database')

const Inventory = require('./Models/Inventory');